services:
  vue-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:80
    volumes:
      - .:/app
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true
